# API server port
APP_PORT=8080

# PostgreSQL connection (placeholder — backend scaffold ready for future features)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=aiteam
